class ItemCollection
  #responsible for holding and searching our Item instances

  #represents one line of data fro mthe items.csv

  def all
    #returns all known items instances
  end

  def where(id)
    #returns either an empty array or an array matching the ID
    
  end
end
